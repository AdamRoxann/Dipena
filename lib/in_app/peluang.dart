import 'package:flutter/material.dart';

class Peluang extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Icon(Icons.local_pizza),
      ),
    );
  }
}